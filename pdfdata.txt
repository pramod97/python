import PyPDF2

# opens and reads the pdf file
x=open('C:\\Users\PRAMOD HARKAR\Desktop\Pro Resume.pdf','rb')

# stores the pdf in the object
pdfread=PyPDF2.PdfFileReader(x)

# prints the no of pages in the file
print(pdfread.numPages)

# gets the page in the object
vobj=pdfread.getPage(0)

# prints the text from the specified page 
print(vobj.extractText())
y=vobj.extractText()